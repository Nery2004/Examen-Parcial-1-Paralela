# Benchmark individual de Blur — Nery

No fue posible ejecutar el benchmark de Blur en esta etapa. El repositorio contiene
únicamente los archivos `.gitkeep` en `secuencial/blur/src/`, `paralelo/blur/src/` y
`data/blur/`: no existen implementaciones ejecutables ni imágenes oficiales
seleccionadas por Renato.

No se crearon algoritmos, imágenes, tiempos ni resultados sustitutos. Las pruebas de
Blur quedan pendientes hasta que el responsable principal aporte código y datos
válidos.
